{
  "name": "express",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mongoose": "^5.13.3",    
    "@angular/cli": "~12.0.1",
    "@angular/compiler-cli": "~12.0.1"
  },
  "devDependencies": {
    "concurrently": "^6.3.0",
    "nodemon": "^2.0.12"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "heroku-postbuild": "cd client && cd Angular && npm install && npm run build",
    "dev": "concurrently \"nodemon index.js\" \"cd client && cd React && npm start\""
  },
  "author": "",
  "license": "ISC"
}
