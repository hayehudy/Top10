{
  "name": "express",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mongoose": "^5.13.3"
  },
  "devDependencies": {
    "concurrently": "^6.3.0",
    "nodemon": "^2.0.12"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "postinstall": "ng build --aot -prod",
    "heroku-postbuild": "cd client && cd Angular && npm install",
    "dev": "concurrently \"nodemon index.js\" \"cd client && cd Angular && ng serve\""
  },
  "author": "",
  "license": "ISC"
}
